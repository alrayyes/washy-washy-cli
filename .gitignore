# dependencies (bun install)
node_modules

# your own chart and appliances. The .dist beside it is the committed
# example; copy it across, or just run the tool and let it fall back
data/washy-washy.json

# output
out
dist
*.tgz

# per-pile card PDFs `bun run examples` also drops into docs/ — the six
# sheets the README links stay committed; these are too many to showcase
docs/washy-washy-card-*.pdf

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# downloaded by bun run prose:sync — the Vale binary, and the style packages it
# lints against. styles/config is ours and stays committed, so the package
# directories are ignored by name rather than the whole styles/ tree
.tools
styles/Google
styles/proselint
